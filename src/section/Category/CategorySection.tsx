import { Headers } from 'components/Elements';

const CategorySection = () => {
   return (
      <div className="max-width padding-x mt-12">
         <Headers>Shop Our Top Categories</Headers>
      </div>
   );
};

export default CategorySection;
